# Production dependencies for Performance Problem Simulator
# Install via: pip install -r requirements.txt

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
psutil>=5.9.0
websockets>=12.0
python-multipart>=0.0.6
httpx>=0.27.0

# Azure Application Insights (optional - for custom events)
# Uses classic SDK (like Java/Node implementations) to avoid conflicts with auto-instrumentation.
opencensus-ext-azure>=1.1.0
