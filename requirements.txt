# Production dependencies for Performance Problem Simulator
# Install via: pip install -r requirements.txt

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
psutil>=5.9.0
websockets>=12.0
python-multipart>=0.0.6
httpx>=0.27.0

# Azure Monitor / Application Insights (optional - for custom events)
# App Service auto-instrumentation requires azure-monitor-opentelemetry to be installed.
# Do NOT call configure_azure_monitor() in code - App Service handles that automatically.
azure-monitor-opentelemetry>=1.0.0
azure-monitor-events-extension>=0.1.0
